{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceRoot}/tidb-server/main.go",
            "env": {},
            "args": ["-config", "${workspaceRoot}/.vscode/config.toml"]
        }
    ]
}